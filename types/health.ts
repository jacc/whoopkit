export type AverageResponse = {
  week_average: string;
  month_average: string;
  six_month_average: string;
};

export type Stress = {
  score: string;
  text: string;
  state: string;
  last_updated: string;
};
